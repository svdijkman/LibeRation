% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-job.R
\name{nm_job_submit}
\alias{nm_job_submit}
\alias{print.nm_job_handle}
\title{Submit an asynchronous \code{nm_est} job}
\usage{
nm_job_submit(
  model,
  data,
  method = "FO",
  label = NULL,
  job_root = nm_job_root(),
  ...
)

\method{print}{nm_job_handle}(x, ...)
}
\arguments{
\item{model}{An \code{nm_model} object.}

\item{data}{An \code{nm_dataset} object.}

\item{method}{Estimation method: \code{"FO"} (two-step: THETA/SIGMA then OMEGA),
\code{"FOCE"}, \code{"SAEM"}, \code{"LAPLACE"}, or \code{"BAYES"} (full Bayesian MCMC).}

\item{label}{Optional short label for the job queue UI.}

\item{job_root}{Job storage directory; defaults to \code{\link{nm_job_root}}.}

\item{...}{Unused.}

\item{x}{A job handle object.}
}
\value{
A list with \code{id}, \code{path}, and \code{process} (a \code{callr} background process).
}
\description{
Runs estimation in a background R process via \pkg{callr}. Poll with
\code{\link{nm_job_status}} and retrieve the fit with \code{\link{nm_job_result}}.
}
\examples{
\dontrun{
if (requireNamespace("callr", quietly = TRUE)) {
  sim <- nm_synthetic_theo(n_sub = 2L)
  job <- nm_job_submit(sim$model, sim$data, method = "FO",
                       control = list(maxit = 3L, compute_inference = FALSE))
  nm_job_status(job$id)
}
}
\dontrun{
if (requireNamespace("callr", quietly = TRUE)) {
  sim <- nm_synthetic_theo(n_sub = 2L)
  job <- nm_job_submit(sim$model, sim$data, method = "FO",
                       control = list(maxit = 3L, compute_inference = FALSE))
  print(job)
}
}
}
