% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/switching-state-space.R
\name{nm_switching_state_space_config}
\alias{nm_switching_state_space_config}
\title{Configure an experimental switching nonlinear state-space model}
\usage{
nm_switching_state_space_config(
  regimes,
  initial_regime,
  regime_transition,
  states,
  initial_mean,
  initial_covariance,
  transition,
  process_covariance,
  observation,
  observation_variance,
  initial_scale = c("probability", "log"),
  transition_scale = c("probability", "log"),
  baseline = c("prediction", "zero"),
  by_dvid = TRUE,
  state_inputs = NULL,
  particles = 512L,
  ess_threshold = 0.5,
  seed = 20260721L,
  dynamics = c("discrete", "sde"),
  sde_method = c("euler", "milstein"),
  sde_substeps = 8L,
  jacobian_step = 1e-05
)
}
\arguments{
\item{regimes}{Unique regime labels.}

\item{initial_regime}{Assignment names for initial regime weights.}

\item{regime_transition}{Square matrix of assignment names for regime
transition weights.}

\item{states}{Unique labels for latent continuous states.}

\item{initial_mean}{Character vector naming the initial-state means.}

\item{initial_covariance}{Square symmetric character matrix naming the
initial covariance entries.}

\item{transition}{List with one next-state/drift assignment vector per regime.}

\item{process_covariance}{List with one covariance/diffusion matrix per regime.}

\item{observation, observation_variance}{One assignment per regime.}

\item{initial_scale, transition_scale}{Probability or log weights.}

\item{baseline}{Add the structural prediction `F` to the latent-state
observation mean, or use a zero baseline.}

\item{by_dvid}{Maintain a separate latent trajectory for each DVID within
subject.}

\item{state_inputs}{Input symbols used for latent states by nonlinear
transition and observation assignments. The default is `STATE_<name>`.}

\item{particles}{Number of particles for the bootstrap filter.}

\item{ess_threshold}{Resample when effective sample size falls below this
proportion of `particles`.}

\item{seed}{Reproducible common-random-number seed for particle inference.}

\item{dynamics}{Discrete transition assignments or continuous-discrete SDE
drift/diffusion assignments. Prefer [nm_sde_config()] for SDE models.}

\item{sde_method}{Fixed-step Euler--Maruyama or diagonal Milstein
propagation for SDE dynamics.}

\item{sde_substeps}{Number of fixed SDE steps per observation interval.}

\item{jacobian_step}{Relative central-difference step used for EKF state
Jacobians. Parameter derivatives are still propagated by CppAD.}
}
\description{
A discrete latent regime is propagated jointly with the continuous state by
the compiled particle filter. A deterministic stratified regime proposal
and differentiable importance weights preserve transition-parameter
gradients, while each regime has its own nonlinear
transition (or SDE drift/diffusion) and observation model. Regime filtering
and genealogical smoothing are returned by [nm_kalman_decode()].
}
