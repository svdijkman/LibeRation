% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-npc-npde.R
\name{nm_add_npc_npde}
\alias{nm_add_npc_npde}
\title{NPC and NPDE via simulation at fixed population parameters}
\usage{
nm_add_npc_npde(
  fit,
  data = fit$data,
  n_sim = 100L,
  seed = 1L,
  refit_eta = TRUE,
  compute_npc = TRUE,
  compute_npde = TRUE,
  pk_engine = NULL,
  n_cores = 1L,
  control = list()
)
}
\arguments{
\item{fit}{An \code{nm_fit} object.}

\item{data}{Optional dataset; defaults to \code{fit$data}.}

\item{n_sim}{Number of simulated datasets.}

\item{seed}{Random seed for simulation.}

\item{refit_eta}{Re-fit subject ETAs on each replicate at fixed \code{fit$par}.}

\item{compute_npc}{If \code{TRUE}, compute NPC values.}

\item{compute_npde}{If \code{TRUE}, compute NPDE values.}

\item{pk_engine}{PK engine for simulation and residual calculation.}

\item{n_cores}{Parallel workers for simulation replicates.}

\item{control}{Passed to eta refit when \code{refit_eta = TRUE}.}
}
\value{
The fit object with \code{gof} columns \code{NPC} and/or \code{NPDE},
and metadata in \code{npc} / \code{npde}.
}
\description{
Monte Carlo predictive check (NPC) and normalized prediction distribution
errors (NPDE) using simulated datasets under the fitted model. Each replicate
uses the same design as the estimation data; conditional residuals (CWRES)
are compared to the observed data.
}
\examples{
\dontrun{
sim <- nm_synthetic_theo(n_sub = 2L, seed = 1L)
fit <- nm_est(sim$model, sim$data, method = "FO",
              control = list(maxit = 5L, compute_inference = FALSE))
fit <- nm_add_npc_npde(fit, n_sim = 5L, refit_eta = FALSE)
}
}
