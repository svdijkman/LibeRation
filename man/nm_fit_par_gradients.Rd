% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-inference.R
\name{nm_fit_par_gradients}
\alias{nm_fit_par_gradients}
\title{Population-parameter gradients at the fit optimum}
\usage{
nm_fit_par_gradients(fit, data = fit$data)
}
\arguments{
\item{fit}{An \code{nm_fit} object.}

\item{data}{Optional dataset; defaults to \code{fit$data}.}
}
\value{
Named numeric vector aligned with \code{fit$par}, or \code{NULL}.
}
\description{
Uses C++ adjoint gradients when supported, otherwise LibeRtAD tape AD.
Numeric fallback is used only when AD is unavailable.
}
\examples{
\dontrun{
sim <- nm_synthetic_theo(n_sub = 2L, seed = 1L)
fit <- nm_est(sim$model, sim$data, method = "FO",
              control = list(maxit = 5L, compute_inference = FALSE))
nm_fit_par_gradients(fit)
}
}
