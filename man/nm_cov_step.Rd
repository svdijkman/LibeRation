% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-covariance.R
\name{nm_cov_step}
\alias{nm_cov_step}
\title{Covariance step at final estimates (NONMEM-style)}
\usage{
nm_cov_step(
  fit,
  data = fit$data,
  method = c("auto", "hessian", "linfim", "sandwich"),
  hessian = c("auto", "ad", "numeric"),
  refit_eta = TRUE,
  update_se = TRUE,
  control = list()
)
}
\arguments{
\item{fit}{An \code{nm_fit} object.}

\item{data}{Optional dataset; defaults to \code{fit$data}.}

\item{method}{\code{"auto"} uses linearized FIM for FO/FOCE/FOCEI and inverse
Hessian otherwise; or \code{"hessian"}, \code{"linfim"}, \code{"sandwich"}.}

\item{hessian}{Hessian source for \code{"hessian"} and \code{"sandwich"}.}

\item{refit_eta}{If \code{TRUE}, re-estimate ETAs at fixed THETA/OMEGA/SIGMA
before computing covariance (recommended).}

\item{update_se}{If \code{TRUE}, set \code{fit$par_se} from \code{method}.}

\item{control}{Passed to ETA refit (e.g. \code{maxit} for inner eta fits).}
}
\value{
Updated \code{nm_fit} object with \code{covariance}, \code{covariance_method},
and optionally refreshed \code{par_se}.
}
\description{
Re-fits subject ETAs at the final population parameters (when applicable),
then computes Hessian, linearized FIM (OPG), and sandwich covariance matrices.
Updates \code{fit$par_se} from the selected method.
}
\examples{
\dontrun{
sim <- nm_synthetic_theo(n_sub = 2L, seed = 1L)
fit <- nm_est(sim$model, sim$data, method = "FO",
              control = list(maxit = 5L, compute_inference = FALSE))
nm_cov_step(fit, refit_eta = FALSE)
}
}
