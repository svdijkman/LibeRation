% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tape-profile.R
\name{nm_ode_tape_benchmark}
\alias{nm_ode_tape_benchmark}
\title{Benchmark nonlinear ODE tape scalability}
\usage{
nm_ode_tape_benchmark(
  model,
  data,
  tolerances = c(1e-05, 1e-07, 1e-09),
  repetitions = 10L,
  jacobian = TRUE,
  checkpoint_repetitions = 64L,
  checkpoint_evaluations = 1000L
)
}
\arguments{
\item{model}{An ADVAN6 or ADVAN13 [nm_model()].}

\item{data}{A NONMEM-style event dataset.}

\item{tolerances}{Positive relative tolerances.}

\item{repetitions}{Repeated evaluation count per tolerance.}

\item{jacobian}{Profile the exact prediction Jacobian.}

\item{checkpoint_repetitions}{Repeated inner-kernel count for the checkpoint
tape-size probe.}

\item{checkpoint_evaluations}{Timed evaluations in the checkpoint probe.}
}
\value{
An `nm_ode_tape_benchmark` list containing `profiles`,
  `checkpoint`, and `assessment`.
}
\description{
Recompiles an ADVAN6 or ADVAN13 model across solver tolerances and profiles
persistent tape size and repeated derivative evaluation. A guarded CppAD
checkpoint probe is returned as decision support; this function does not
silently switch production propagation kernels.
}
