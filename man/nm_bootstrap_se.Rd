% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-bootstrap.R
\name{nm_bootstrap_se}
\alias{nm_bootstrap_se}
\title{Bootstrap standard errors for population parameters}
\usage{
nm_bootstrap_se(fit, n_boot = 50L, seed = 1L, warm_start = TRUE, ...)
}
\arguments{
\item{fit}{An \code{nm_fit} object (used for model, data, method, controls).}

\item{n_boot}{Number of bootstrap replicates.}

\item{seed}{Random seed.}

\item{warm_start}{If \code{TRUE} (default), start each replicate from the
original fit parameters; otherwise use control-file initials.}

\item{...}{Passed to \code{nm_est} (excluding \code{data}).}
}
\value{
A list with \code{se}, \code{bias}, \code{par_mean}, and \code{boot_pars}.
}
\description{
Nonparametric bootstrap: resample subjects with replacement, refit, summarise.
}
\examples{
\dontrun{
sim <- nm_synthetic_theo(n_sub = 2L, seed = 1L)
fit <- nm_est(sim$model, sim$data, method = "FO",
              control = list(maxit = 5L, compute_inference = FALSE))
nm_bootstrap_se(fit, n_boot = 3L, control = list(maxit = 3L))
}
}
