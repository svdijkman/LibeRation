% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmm.R
\name{nm_cthmm_config}
\alias{nm_cthmm_config}
\title{Configure a continuous-time hidden Markov model}
\usage{
nm_cthmm_config(
  states,
  initial,
  generator,
  emission,
  initial_scale = c("probability", "log"),
  rate_scale = c("rate", "log"),
  emission_scale = c("likelihood", "log"),
  by_dvid = TRUE
)
}
\arguments{
\item{states}{Unique labels for the hidden states.}

\item{initial}{Character vector naming one `$ERROR` initial-state assignment
per state.}

\item{generator}{Square character matrix. Every off-diagonal element names
the `$ERROR` assignment for the corresponding from-state to to-state rate.
Diagonal elements are ignored and may be empty or `NA`.}

\item{emission}{Character vector naming one state-conditional `$ERROR`
emission assignment per state.}

\item{initial_scale}{Whether `initial` contains probability weights or log
weights.}

\item{rate_scale}{Whether off-diagonal generator assignments contain
non-negative rates or unconstrained log rates.}

\item{emission_scale}{Whether `emission` contains likelihoods/densities or
log likelihoods.}

\item{by_dvid}{Maintain independent state sequences for each `DVID` within
subject.}
}
\value{
A validated, serializable continuous-time hidden Markov
  configuration.
}
\description{
`nm_cthmm_config()` is the continuous-time counterpart of
[nm_hmm_config()]. Off-diagonal entries of `generator` name transition-rate
assignments in `$ERROR`; diagonal entries are generated as minus the sum of
the other entries in their row. At every observation the compiled engine
calculates `exp(Q * DT)` with a CppAD-compatible matrix exponential. This
supports irregular observation times, time-varying rates, exact gradients,
retrospective smoothing, and Viterbi decoding.
}
