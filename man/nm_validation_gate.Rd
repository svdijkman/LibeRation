% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation-gates.R
\name{nm_validation_gate}
\alias{nm_validation_gate}
\title{Require numerical agreement before publishing a benchmark}
\usage{
nm_validation_gate(
  reference,
  candidate,
  reference_predictions = NULL,
  candidate_predictions = NULL,
  tolerances = list(objective = list(absolute = 0.001, relative = 1e-04), theta =
    list(absolute = 1e-05, relative = 0.02), omega = list(absolute = 1e-05, relative =
    0.1), sigma = list(absolute = 1e-05, relative = 0.1), eta = list(absolute = 1e-04,
    relative = 0.1), predictions = list(absolute = 1e-06, relative = 0.02)),
  required = c("objective", "theta", "omega", "sigma", "eta", "predictions")
)
}
\arguments{
\item{reference, candidate}{Reference-engine and LibeRation fit summaries or
`nm_fit` objects.}

\item{reference_predictions, candidate_predictions}{Optional prediction
vectors, avoiding a fresh `predict()` call.}

\item{tolerances}{Named per-component absolute/relative tolerances.}

\item{required}{Components that must be present and pass.}
}
\value{
An `nm_validation_gate` report.
}
\description{
Require numerical agreement before publishing a benchmark
}
