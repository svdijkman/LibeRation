% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-workspace.R
\name{nm_workspace_import_dataset}
\alias{nm_workspace_import_dataset}
\title{Copy a dataset file into a project's \code{data/} folder}
\usage{
nm_workspace_import_dataset(
  project,
  source_path,
  dest_name = NULL,
  root = nm_workspace_root()
)
}
\arguments{
\item{project}{Project name.}

\item{source_path}{Path to an existing file on disk.}

\item{dest_name}{Optional basename for the copied file (defaults to source basename).}

\item{root}{Workspace root.}
}
\value{
Relative path suitable for \code{$DATA} (e.g. \code{"data/theo.csv"}).
}
\description{
Copy a dataset file into a project's \code{data/} folder
}
\examples{
\dontrun{
ws <- tempfile()
nm_workspace_init(ws, create_demo_project = FALSE)
nm_workspace_create_project("demo", path = ws, template = "empty")
sim <- nm_synthetic_theo(n_sub = 2L)
csv <- tempfile(fileext = ".csv")
write.csv(sim$data$data, csv, row.names = FALSE)
nm_workspace_import_dataset("demo", csv, root = ws)
}
}
