% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-sim-ext.R
\name{nm_sim_design_dataset}
\alias{nm_sim_design_dataset}
\title{Build a simulation template dataset from a dosing / sampling design}
\usage{
nm_sim_design_dataset(
  model,
  n_sub = 10L,
  n_days = 1L,
  obs_times = NULL,
  obs_per_day = 8L,
  dose_mode = c("single", "repeat", "steady_state"),
  dose_amt = 320,
  dose_table = NULL,
  dose_n = 3L,
  dose_ii = 12,
  dose_cmt = 1L,
  template_data = NULL,
  seed = NULL
)
}
\arguments{
\item{model}{An \code{nm_model} object.}

\item{n_sub}{Number of subjects.}

\item{n_days}{Simulation horizon in days (TIME assumed in hours; 24 h per day).}

\item{obs_times}{Observation times (hours); default from \code{template_data} or a PK grid.}

\item{dose_mode}{\code{"single"}, \code{"repeat"}, or \code{"steady_state"}.}

\item{dose_amt}{Scalar dose amount when \code{dose_table} is empty.}

\item{dose_table}{Optional data frame or parsed text lines with \code{TIME} and \code{AMT}.}

\item{dose_n}{Number of doses for repeat dosing.}

\item{dose_ii}{Inter-dose interval (hours).}

\item{dose_cmt}{Dosing compartment.}

\item{template_data}{Optional \code{nm_dataset} to inherit observation times / amounts.}
}
\description{
Build a simulation template dataset from a dosing / sampling design
}
\examples{
sim <- nm_synthetic_theo(n_sub = 2L)
nm_sim_design_dataset(sim$model, sim$data, n_sub = 2L, seed = 1L)
}
