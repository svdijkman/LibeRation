% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hmm.R
\name{nm_hmm_config}
\alias{nm_hmm_config}
\title{Configure a finite-state hidden Markov model}
\usage{
nm_hmm_config(
  states,
  initial,
  transition,
  emission,
  initial_scale = c("probability", "log"),
  transition_scale = c("probability", "log"),
  emission_scale = c("likelihood", "log"),
  by_dvid = TRUE
)
}
\arguments{
\item{states}{Unique labels for the hidden states.}

\item{initial}{Character vector naming one `$ERROR` assignment per state.}

\item{transition}{Character matrix naming `$ERROR` assignments for each
from-state (row) to-state (column) transition.}

\item{emission}{Character vector naming one state-conditional `$ERROR`
emission assignment per state.}

\item{initial_scale}{Whether `initial` assignments contain non-negative
weights or log weights.}

\item{transition_scale}{Whether `transition` assignments contain
non-negative weights or log weights.}

\item{emission_scale}{Whether `emission` assignments contain positive
likelihoods/densities or log likelihoods.}

\item{by_dvid}{Maintain independent hidden-state sequences for each `DVID`
within subject. If false, all observations within subject form one
sequence.}
}
\value{
A validated, serializable hidden Markov configuration.
}
\description{
`nm_hmm_config()` maps assignments in a model's `$ERROR` block to the
initial-state weights, transition weights, and state-conditional emission
likelihoods used by LibeRation's compiled forward algorithm. Initial and
transition weights are normalized internally, so users may supply either
probabilities or positive unnormalised weights. Log weights are normalized
with a stable softmax. Emissions may be probabilities, densities, or log
likelihoods.
}
