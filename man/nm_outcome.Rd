% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome.R
\name{nm_outcome}
\alias{nm_outcome}
\title{Declare a first-class pharmacometric outcome}
\usage{
nm_outcome(
  family = c("normal", "lognormal", "student_t", "bernoulli", "categorical", "ordinal",
    "poisson", "negative_binomial", "binomial", "zero_inflated_poisson",
    "hurdle_poisson", "tte", "recurrent_event", "competing_risks", "markov",
    "continuous_time_markov"),
  name = NULL,
  dvid = NULL,
  prediction = "F",
  scale = NULL,
  probabilities = NULL,
  categories = NULL,
  trials = NULL,
  dispersion = NULL,
  zero_probability = NULL,
  event = 1,
  cause_hazards = NULL,
  initial = NULL,
  transition = NULL,
  rates = NULL,
  df = 4,
  max_count = 100L,
  risk = NULL
)
}
\arguments{
\item{family}{Observation family. Supported families are Gaussian,
log-normal, fixed-degrees-of-freedom Student t, Bernoulli, categorical,
ordinal, Poisson, negative binomial, binomial, zero-inflated Poisson,
hurdle Poisson, first/recurrent time-to-event, competing risks, and
observed finite-state Markov models, and exact two-state continuous-time
Markov models.}

\item{name}{Human-readable endpoint name.}

\item{dvid}{Optional numeric `DVID` selecting this endpoint. It is required
when several outcomes are combined.}

\item{prediction}{Name of a `$PK/$PRED` assignment containing the mean,
probability, intensity, or hazard. `F` uses the engine prediction.}

\item{scale}{Residual SD for continuous families, as a number, a named
assignment, or `THETA(i)`/`SIGMA(i)`.}

\item{probabilities}{Named `$PK/$PRED` probability assignments for
categorical or ordinal outcomes.}

\item{categories}{Numeric observed category codes corresponding to
`probabilities`.}

\item{trials}{Number of binomial trials, either fixed or supplied by a
numeric dataset/model symbol.}

\item{dispersion}{Negative-binomial size parameter.}

\item{zero_probability}{Structural-zero probability for ZIP/hurdle models.}

\item{event}{Numeric event code for TTE models.}

\item{cause_hazards}{Named hazard assignments for competing risks; names
are the numeric cause codes.}

\item{initial}{Initial-state probability assignments for Markov models.}

\item{transition}{Square matrix of transition-probability assignments for
Markov models; rows are previous states and columns are current states.}

\item{rates}{Two named off-diagonal intensity assignments (`q01`, `q10`)
for a two-state continuous-time Markov model.}

\item{df}{Fixed Student-t degrees of freedom.}

\item{max_count}{Maximum supported count used to unroll exact factorial and
negative-binomial normalizing terms on the AD tape.}

\item{risk}{Optional at-risk indicator/expression for event models.}
}
\value{
An `nm_outcome` declaration.
}
\description{
`nm_outcome()` describes an observation distribution independently of the
structural PK/PD model. LibeRation translates the declaration into editable
`$ERROR` code, compiles it with CppAD, and retains enough metadata for
stochastic simulation and outcome-appropriate diagnostics. Several
declarations can be combined with [nm_outcomes()] and selected by `DVID`.
}
