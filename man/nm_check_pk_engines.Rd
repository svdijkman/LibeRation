% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-cpp.R
\name{nm_check_pk_engines}
\alias{nm_check_pk_engines}
\title{Compare the R and C++ PK engines for the same inputs (divergence notice)}
\usage{
nm_check_pk_engines(
  model,
  data,
  theta = NULL,
  omega = NULL,
  sigma = NULL,
  eta = NULL,
  tol = 1e-04,
  warn = TRUE
)
}
\arguments{
\item{model}{An \code{nm_model} object.}

\item{data}{An \code{nm_dataset} or data.frame.}

\item{theta, omega, sigma}{Optional parameter vectors (default: model inits).}

\item{eta}{Optional per-subject ETA matrix (default: zeros).}

\item{tol}{Relative tolerance for flagging divergence.}

\item{warn}{Logical; emit a one-time warning when diverged.}
}
\value{
Invisibly, a list with \code{max_abs_diff}, \code{max_rel_diff},
\code{diverged}, and a per-subject \code{data.frame}.
}
\description{
Predicts each subject's concentrations with both the R and the C++ PK solvers
at the supplied parameters and reports the largest absolute and relative
disagreement. If the engines disagree by more than \code{tol} (relative) a
one-time warning (the "divergence notice") is emitted. This is a diagnostic
safety check: the two engines should agree to numerical tolerance, and a
large divergence indicates a model/data combination where one engine is
unreliable.
}
\examples{
\dontrun{
sim <- nm_synthetic_theo(n_sub = 3L)
nm_check_pk_engines(sim$model, sim$data)
}
}
