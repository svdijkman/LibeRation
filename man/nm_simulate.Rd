% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{nm_simulate}
\alias{nm_simulate}
\title{Simulate or predict a LibeRation model}
\usage{
nm_simulate(
  model,
  data,
  theta = NULL,
  eta = NULL,
  sigma = NULL,
  omega = NULL,
  nsim = 1L,
  random_effects = FALSE,
  residual = FALSE,
  sample_mixture = FALSE,
  censor = FALSE,
  seed = NULL,
  n_cores = 1L
)
}
\arguments{
\item{model}{An `nm_model` or compiled `NMEngine`.}

\item{data}{NONMEM-style event data.}

\item{theta}{Population fixed effects; defaults to the model table.}

\item{eta}{Subject ETA matrix ordered by first subject appearance.}

\item{sigma}{Residual parameters.}

\item{omega}{Random-effect covariance parameters.}

\item{nsim}{Number of simulation replicates.}

\item{random_effects}{Sample subject/occasion ETAs from OMEGA when `eta` is
not supplied.}

\item{residual}{Sample observations from the configured residual model.}

\item{sample_mixture}{Sample finite-mixture membership per subject.}

\item{censor}{Mark simulated values below LLOQ as BLQ/CENS and replace their
reported DV by the applicable quantification limit.}

\item{seed}{Optional reproducible RNG seed.}

\item{n_cores}{Number of parallel simulation workers. Replicates are
distributed across persistent PSOCK workers on Windows, Linux, and macOS.}
}
\value{
Input records augmented with `IPRED`, compartment amounts, and—when
  requested—simulated `DV`, ETAs, mixture membership, and replicate number.
}
\description{
Simulate or predict a LibeRation model
}
\examples{
\donttest{
model <- nm_model(
  INPUT = c("ID", "TIME", "EVID", "AMT", "CMT", "DV", "MDV"),
  ADVAN = 1,
  PRED = "CL=THETA(1); V=THETA(2); S1=V",
  ERROR = "Y=F",
  THETAS = data.frame(THETA = 1:2, Value = c(2, 20))
)
events <- data.frame(
  ID = 1, TIME = c(0, 1, 2), EVID = c(1, 0, 0),
  AMT = c(100, 0, 0), CMT = 1, DV = NA_real_, MDV = c(1, 0, 0)
)
nm_simulate(model, events)
}
}
