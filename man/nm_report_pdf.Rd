% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-report.R
\name{nm_report_pdf}
\alias{nm_report_pdf}
\title{Generate a model analysis report (PDF)}
\usage{
nm_report_pdf(
  fit,
  output_path,
  sections = list(summary = TRUE, parameters = TRUE, gof_time = TRUE, gof_ipred_time =
    TRUE, gof_scatter = TRUE, gof_residuals = TRUE, diag_shrinkage = TRUE, diag_eta =
    TRUE, narrative_stub = TRUE),
  project_meta = list()
)
}
\arguments{
\item{fit}{An \code{nm_fit} object.}

\item{output_path}{Output \code{.pdf} path (must be inside workspace when called from GUI).}

\item{sections}{List of logical flags: \code{summary}, \code{parameters},
\code{gof_time}, \code{gof_ipred_time}, \code{gof_scatter}, \code{gof_residuals},
\code{diag_shrinkage}, \code{diag_eta}, \code{narrative_stub}.}

\item{project_meta}{Optional list with \code{project}, \code{run_id}, \code{workspace}.}
}
\value{
Invisibly, list with \code{pdf} and \code{manifest} paths.
}
\description{
Designed for pharmacometric workflows and future AI narrative extensions.
Writes a PDF plus a JSON manifest alongside for agent consumption.
}
\examples{
\dontrun{
sim <- nm_synthetic_theo(n_sub = 2L, seed = 1L)
fit <- nm_est(sim$model, sim$data, method = "FO",
              control = list(maxit = 5L, compute_inference = FALSE))
out <- tempfile(fileext = ".pdf")
nm_report_pdf(fit, out, sections = list(summary = TRUE, parameters = TRUE))
}
}
