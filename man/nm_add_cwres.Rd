% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-cwres.R
\name{nm_add_cwres}
\alias{nm_add_cwres}
\title{FOCEI conditional residuals and objective on any fit (nlmixr2-style addCwres)}
\usage{
nm_add_cwres(
  fit,
  data = fit$data,
  refit_eta = FALSE,
  pk_engine = NULL,
  control = list()
)
}
\arguments{
\item{fit}{An \code{nm_fit} object.}

\item{data}{Optional dataset; defaults to \code{fit$data}.}

\item{refit_eta}{If \code{TRUE}, re-fit subject ETAs at \code{fit$par} before GOF.}

\item{pk_engine}{PK engine for predictions and optional eta refit.}

\item{control}{Passed to eta refit when \code{refit_eta = TRUE}.}
}
\value{
The fit object with \code{gof} (row-level table), \code{cwres_obj},
and \code{cwres_eta} attached.
}
\description{
Adds population predictions, conditional predictions (CPRED), conditional
residuals (CRES, CWRES), and a FOCEI objective evaluated at the fit's
post-hoc ETAs. Works for FO, FOCE, SAEM, etc., not only FOCEI fits.
}
\examples{
\dontrun{
sim <- nm_synthetic_theo(n_sub = 2L, seed = 1L)
fit <- nm_est(sim$model, sim$data, method = "FO",
              control = list(maxit = 5L, compute_inference = FALSE))
fit <- nm_add_cwres(fit)
head(fit$gof[, c("ID", "TIME", "CWRES")])
}
}
