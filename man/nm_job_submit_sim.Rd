% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-job.R
\name{nm_job_submit_sim}
\alias{nm_job_submit_sim}
\title{Submit an asynchronous simulation job}
\usage{
nm_job_submit_sim(
  model,
  data,
  project,
  version_id,
  sim_id,
  n_sim = 1L,
  seed = 1L,
  n_cores = 1L,
  pk_engine = "cpp",
  theta = NULL,
  omega = NULL,
  sigma = NULL,
  label = NULL,
  use_fit = FALSE,
  est_run_id = NULL,
  design = NULL,
  vpc = FALSE,
  sim_compute_npc = FALSE,
  sim_compute_npde = FALSE,
  diag_n_sim = 50L,
  diag_refit_eta = TRUE,
  diag_only = FALSE,
  workspace_root = nm_workspace_root(),
  job_root = nm_job_root(),
  server = NULL,
  fit = NULL
)
}
\arguments{
\item{model}{An \code{nm_model} object.}

\item{data}{Template \code{nm_dataset}.}

\item{project}{Workspace project name (simulation saved under this project).}

\item{version_id}{Model version id.}

\item{sim_id}{Pre-allocated simulation id from \code{\link{nm_workspace_new_sim_id}}.}

\item{n_sim}{Number of simulated datasets.}

\item{seed}{Random seed.}

\item{n_cores}{Number of parallel workers for replicates (\code{1L} = sequential).}

\item{pk_engine}{PK engine passed to \code{.nm_task_sim} (\code{"cpp"} recommended).}

\item{workspace_root}{Workspace root passed to the worker for saving results.}
}
\description{
Submit an asynchronous simulation job
}
\examples{
\dontrun{
if (requireNamespace("callr", quietly = TRUE)) {
  ws <- tempfile()
  nm_workspace_init(ws, create_demo_project = FALSE)
  nm_workspace_create_project("demo", path = ws, template = "theo")
  ver <- nm_workspace_list_versions("demo", root = ws)[[1L]]
  sim <- nm_synthetic_theo(n_sub = 2L)
  sim_id <- nm_workspace_new_sim_id("demo", ver, root = ws)
  nm_job_submit_sim(sim$model, sim$data, "demo", ver, sim_id,
                    n_sim = 1L, workspace_root = ws)
}
}
}
