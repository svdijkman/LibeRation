% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-profile-likelihood.R
\name{nm_profile_likelihood}
\alias{nm_profile_likelihood}
\alias{print.nm_profile_likelihood}
\title{Likelihood profiling of a THETA parameter}
\usage{
nm_profile_likelihood(
  fit,
  parameter,
  grid = NULL,
  n = 7L,
  span = 0.3,
  delta_ofv = 3.84,
  method = NULL,
  control = list(),
  ...
)

\method{print}{nm_profile_likelihood}(x, ...)
}
\arguments{
\item{fit}{A converged \code{nm_fit} object.}

\item{parameter}{THETA to profile: an integer THETA index, or a label such as
\code{"THETA2"}.}

\item{grid}{Optional numeric vector of values to profile over. When
\code{NULL} a symmetric grid of \code{n} points spanning
\code{estimate * (1 +/- span)} is used.}

\item{n}{Number of grid points when \code{grid} is \code{NULL}.}

\item{span}{Fractional half-width of the default grid around the estimate.}

\item{delta_ofv}{Change in OFV defining the CI (default 3.84 = 95\\%).}

\item{method}{Estimation method for the re-optimisations (default: the
method of \code{fit}).}

\item{control}{Control list passed to \code{\link{nm_est}}
(\code{compute_inference = FALSE} is forced).}

\item{...}{Unused.}

\item{x}{An \code{nm_profile_likelihood} object.}
}
\value{
A list with class \code{nm_profile_likelihood}: \code{profile}
(data.frame of \code{value}, \code{ofv}, \code{dofv}), \code{estimate},
\code{ci} (named \code{lower}/\code{upper}), \code{delta_ofv}, and
\code{parameter}.
}
\description{
Profiles the objective function value (OFV, -2 log-likelihood) over a grid of
values for a chosen \code{THETA}, re-optimising all the other parameters at
each grid point (the profiled parameter is held fixed). Returns the profile
and a likelihood-based confidence interval defined by the change in OFV
(\eqn{\Delta OFV = 3.84} for a 95\\% CI, the \eqn{\chi^2_1} quantile).
}
\details{
This is likelihood-based \emph{parameter} profiling. For the runtime timing
profiler see \code{\link{nm_time_profile}}.
}
\examples{
\dontrun{
sim <- nm_synthetic_theo(n_sub = 20L, seed = 1L)
fit <- nm_est(sim$model, sim$data, method = "FO")
pl <- nm_profile_likelihood(fit, "THETA1")
pl$ci
}
}
