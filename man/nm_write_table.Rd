% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nm-table-export.R
\name{nm_write_table}
\alias{nm_write_table}
\title{Write a NONMEM-style $TABLE output file for a fit}
\usage{
nm_write_table(
  fit,
  file = NULL,
  columns = NULL,
  data = fit$data,
  firstonly = FALSE,
  nonmem_header = FALSE,
  digits = 6L
)
}
\arguments{
\item{fit}{An \code{nm_fit} object.}

\item{file}{Output path. If \code{NULL} the table is returned but not written.}

\item{columns}{Character vector of columns to export. Defaults to the NONMEM
standard set (\code{ID TIME DV PRED IPRED RES WRES CWRES}) followed by the
ETA columns. Unknown columns are dropped with a warning.}

\item{data}{Optional dataset (defaults to \code{fit$data}).}

\item{firstonly}{Logical; if \code{TRUE} write one row per subject (the first
record), mimicking NONMEM's \code{FIRSTONLY}.}

\item{nonmem_header}{Logical; if \code{TRUE} prepend a \code{TABLE NO. 1}
banner line (NONMEM convention). Default \code{FALSE}.}

\item{digits}{Number of significant digits for numeric formatting.}
}
\value{
Invisibly, the exported \code{data.frame}.
}
\description{
Exports a whitespace-delimited table (with a header line) of the standard
NONMEM diagnostic quantities for an \code{nm_fit}: \code{ID TIME DV PRED
IPRED RES WRES CWRES} plus the empirical-Bayes \code{ETA1..ETAn}. Additional
quantities produced by \code{\link{nm_add_cwres}} (\code{CPRED}, \code{CRES},
\code{IWRES}) are also available.
}
\examples{
\dontrun{
sim <- nm_synthetic_theo(n_sub = 3L, seed = 1L)
fit <- nm_est(sim$model, sim$data, method = "FOCE",
              control = list(maxit = 10L, compute_inference = FALSE))
tab <- nm_write_table(fit, tempfile(fileext = ".tab"))
head(tab)
}
}
